import Foundation
import ReactorKit

class SignUpReactor: Reactor {

    private let disposeBag: DisposeBag = .init()
    let initialState: State

    enum Action {
    }
    enum Mutation {
    }
    struct State {
    }

    init() {
        initialState = State()

    }

}
